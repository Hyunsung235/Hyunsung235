!!! p1의 방향과 p2의 방향이 꼬였음 >> 원본사진의 캐릭터가 바라보는 방향이 다르다보니 혼동이 생겼음 !!!
    >>> 방향을 주의할 것.

!! 표창을 회전시키고 싶으나, 회전할 경우 렉이 심하게 발생하였음 !!
    >>> 표창도 회전하는 것을 사진에서 처리하거나, 렉이 발생하는 원인을 잡아야 함.


!! HP게이지바의 상수는 건들지 말 것 >> 배경에 뚫어져 있는 칸과 정확히 맞춰 놓았으니 건들면 싱크가 안 맞음 !!

!! 캐릭터의 사진 크기를 건들지 말 것 >> 캐릭터의 사진에 있는 좌표에 맞춰 이미지를 불러오니 캐릭터의 사진 크기 변경시 애니메이션이 뒤틀림 !!


import pygame
import sys
import math
import numpy
에서 numpy 모듈과 pygame 모듈은 좌측상단 버튼 - 파일 - 설정 - IvsI - 인터프리터 - +버튼 에서 다운로드하여야 함